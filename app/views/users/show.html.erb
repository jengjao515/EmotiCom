<div class="row">
	<div class="col-md-8 user-top">

		Hi, I am logged in. <%= @user.name %>

		<%= link_to "Create a Post", new_post_path %>
		<%= link_to "Logout", logout_path %>
	
		<p><% @posts.each do |post| %>
			<%= post.description %><br>
		<% end %></p>
	</div>
</div>
