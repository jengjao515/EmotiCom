<div class="row">
	<div class="col-md-5">
		<h1>UserHomePage</h1>
		Hi, I am logged in.
		<%= @user.name %>

		<%= link_to "Create a Post", new_post_path %>
		<%= link_to "Logout", logout_path %>

		<% @posts.each do |post| %>
		<%= post.description %><br>
	</div>
<% end %>
</div>